Add a cli command where you pass it a url and it:
- downloads the page / pdf
- extracts the text / converts to markdown
- stores it in the repo
- stores the url somewhere too (maybe markdown metadata?)